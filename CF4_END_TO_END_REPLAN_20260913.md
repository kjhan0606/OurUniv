# 최종목표 중심 재설계 — 2026-09-13

상태: **사용자가 2026-09-13 내부 IC 공동추론 경로와 R1 실행을 명시적으로 승인함.**
운전자 검토와 Fable 다중 자문을 반영한다. 이 문서 자체는 새 학습·대형
시뮬레이션의 완료 기록이 아니다. R1 실행 기록은 [CF4_R1_RUN.md](CF4_R1_RUN.md).
기존 자료·실패 결과·승인 기록은 보존한다.

자문: Fable5에 과학 경로, LG/줌 구현, 근거 교정 후 종합안의 **3건**을 의뢰했다.
같은 모델의 자문 세 건이며 서로 다른 세 감사자의 합의가 아니다.
채택·수정·기각한 내용은 [자문 처리 기록](CF4_END_TO_END_ADVICE_20260913.md)에 남긴다.

## 1. 결론과 바꾸지 않는 목적

목표는 범용 초해상도 신경망이 아니라 **실제 국부우주 관측에 조건부인,
동역학적으로 가능한 현재 상태와 그 상태를 만드는 LG 줌 초기조건의 앙상블**이다.
주변은 1–2 cMpc/h, LG 지도는 <=0.3 cMpc/h. MW·M31·M33의 관측 위치,
거리, 질량에 관한 자료, 상대속도와 국부 환경을 불확실성과 함께 만족시킨다.
관측에 없는 미세 위상은 조건부 사전분포로 남긴다. 고유한 과거의 복원,
모든 은하의 개별 복원, 전 상자의 0.3 해상도는 약속하지 않는다.

**현재 계산을 더 오래 학습시키는 방식으로 목표에 도달할 근거는 부족하다.**
반면 유사한 제약 우주론 시뮬레이션의 선례는 있으므로 목표 자체를 불가능으로
판정할 근거도 없다. MW–M31/주변 흐름의 공동추론은 문헌에서 입증되었지만,
우리의 CF4 전역 환경 + M33 + 줌 산출물 전체가 이미 해결됐다는 뜻은 아니다.
[Wempe et al. 2024](https://arxiv.org/abs/2406.02228),
[SIBELIUS-DARK](https://arxiv.org/abs/2202.04099).

## 2. 실제로 확보한 것과 빠진 것

| 확보한 자산 | 사용할 곳 | 아직 보장하지 않는 것 |
| --- | --- | --- |
| 실제 CF4·2M++ 입력, 선택함수/단위 수정, 12 cMpc/h 진단 지도 | 관측 likelihood와 전역 비교 기준 | 1–2 cMpc/h의 검증된 posterior |
| TNG 총물질 입자·격자·SUBFIND, 질량/운동량/2차 모멘트 보존 | 관측 연산자와 수치 오차 교정 | CF4 조건부 우주론 사전분포 |
| 실제 LG 거리·시선속도·고유운동 및 동일 장 위치 likelihood | 관측 변환과 공통 오차 처리 | 물리적 M33 질량·COM·존재의 추론 |
| 2개 모의 현재장→IC→PM 최적화 시험 | 미분·단위·초기화 재사용 | posterior sampling, 미세 헤일로, 독립 검증 |
| HOP·RAMSES·입자 추적·GRAFIC/줌 코드 | 검증 후 구조 판독과 출력 | 새 위상/질량/힘 해상도에서의 정합성 |

352623은 2026-09-13 19:36:34 KST 종료(8분21초). 6000회 전용 학습,
검사4개와 분기검사3개 통과, 정상 생성16/16이나 개별 품질1/16·평균 품질2/8.
첫 세분화의 평균속도 변동에 들어간 분산 비율은 0.22645, 원자료0.09429.
**해상도 전용 학습까지 포함한 현재 수정 계열은 종료한다.**

계획상 잘못은 다음과 같다.

- 독립적인 고분해능 생성기의 완성을 전체 과학 경로의 필수 조건으로 삼았다.
- 보존되는 7개 모멘트를 만들면 우주론적 형성 이력도 존재할 것처럼 접근했다.
- 위치 법칙의 성능을 물리적 헤일로/서브헤일로의 식별에 너무 가깝게 해석했다.
- 같은 작은 개발 표본에서 반복한 통과율을 과학 목표의 진척으로 과대평가했다.
- Slurm 작업 안의 학습→평가 자동화와 작업 종료 후 운전자의 자동 재개를
  구분하지 않고 연속 진행을 약속했다. 후자는 실제로 작동하지 않았다.

기존 품질 미달 기록은 유지한다. 단, 확률적 생성 표본마다 하나의 원자료와
모든 통계가 비슷해야 한다는 기준 자체도 보정된 posterior 검증은 아니다.
향후에는 독립 모의 관측의 coverage·예측 성능·동역학 수렴을 사용하며,
옛 실패를 통과시키려고 임계값을 바꾸지는 않는다.

## 3. 가장 중요한 설계 선택 — 숨기지 않는다

**추천: 현재 상태와 형성 이력을 하나의 동역학적 확률모형으로 묶는다.**

관측 → [현재 입자 상태·밀도·속도와 형성 이력의 공동 posterior]
→ **현재장 지도/불확실성을 먼저 공개** → 그 표본에 딸린 IC → 정밀 전방검증 → 줌.

이 구현은 내부에서 초기조건을 추론한다. 수학적으로 **Bayesian forward
IC inference**이며, 이것을 전혀 새로운 “IC를 쓰지 않는 현재장 복원”이라고
부르지 않는다. 달라지는 것은 실제 LG 관측을 같은 물리 상태에 연결하고,
현재장 posterior를 먼저 검증·제공하며, 임의 high-k/최선 seed 선택으로
LG를 대신하지 않는다는 점이다. 과거의 코드·결과를 그대로 재승격하지 않는다.

독립 ML로 현재 밀도·평균속도·분산을 만든 뒤 이를 단순 역적분하면,
다중 흐름의 입자 분포/라그랑지안 소속/성장모드 이력을 정할 정보가 부족하다.
정확한 입자 위상공간의 역학이 원천적으로 비가역이라는 뜻은 아니다.
또한 모든 모멘트 지도가 “확률0의 비물리 상태”라는 주장도 입증되지 않았다.
문제는 **현재 방법으로 동역학적 호환성과 올바른 이력의 확률을 보장하지
못한다는 것**이다. 독립 현재장→조건부 이력 방식도 이론적으로 가능하지만,
일관된 현재장 사전분포와 조건부 이력 분포가 필요하며 지금 둘 다 없다.

사용자가 내부 IC 공동추론도 원하지 않는다면, 이 추천 경로는 실행하지 않는다.
그 경우 독립 z=0 사전분포/조건부 이력 모형을 개발하는 더 불확실한 연구 경로와
기간을 새로 합의해야 한다. 이름만 바꾸어 우회하지 않는다.

### 확률 대상과 데이터 사용

`s`: 전역·국부의 위상이 연결된 백색잡음 자유도, `theta`: 관측/수치/기준계
불확실성, `A`: 관측 은하와 시뮬레이션 성분의 잠재 대응.
`F(s)`: 같은 LCDM 전이함수와 성장모드에서 진화한 입자 상태.

`p(s,theta,A | D) ∝ N(s;0,I) p(theta) p(A | F(s),S)
                   × L(D_CF4,D_gal,D_LG | F(s),theta,A,S)`

`S`는 선택/관측자 규약이다. 관측들 사이 공유 오차는 공동 likelihood로
처리한다. 조건부 독립을 확인한 항만 곱한다. 물질지도는 `F(s)`의 판독값이고,
수치/바리온 차이는 관측 예측의 discrepancy로 교정한다. 자유로운 임의 지도
보정을 넣어 동역학적 연결을 끊지 않는다. cosmology는 우선 고정하고 민감도를
후속 비교한다. LCDM P(k)는 표본마다 인위적으로 맞추지 않는다.
초기 `theta`는 필요한 소수의 보정/불확실성 변수로 제한한다. 이 식은 설계 대상이지
구현 완료 선언이 아니다. 특히 전역 1.5 격자에서 MW/M31/M33가 물리적으로 분해된
헤일로라고 가정할 수 없으며, 미분 가능한 근사 관측 연산자와 실제 bound 성분의
대응 오차는 아직 교정해야 한다.

- 기존 12 cMpc/h 모델은 PM으로 교정한 **z=0 Gaussian covariance 근사**다.
  기존 sampler를 큰 PM posterior로 단순 확대할 수는 없다.
- 환경 posterior 뒤 LG 자료를 추가하면 국부 자유도뿐 아니라 전역 자유도도
  갱신한다. 고정 parent는 예비 근사이며 최종 posterior가 아니다.
- 구모델 posterior를 proposal로 사용하면 새/구 prior·likelihood 비와 support를
  처리해야 한다. 어렵다면 초기화에만 쓰고 새 대상에서 다시 표본추출한다.
- LG를 반영한 뒤 같은 LG 조건으로 또 가중하지 않는다. 정밀 solver로 바꿀 때는
  새/구 likelihood 비 또는 새 대상의 재추론이다. 좋은 표본만 남기는 것은 별도
  선택이며 전체 posterior처럼 보고하지 않는다.

## 4. 다섯 개 결과물 묶음

각 묶음 내부를 승인/감사 micro-stage로 나누지 않는다. 필수 구현·작은 검사·
계산·결과 보고를 하나로 수행한다. 실패 시 같은 하이퍼파라미터 순회를 하지 않는다.

| 묶음 | 반드시 남길 결과물 | 다음으로 갈 판단 |
| --- | --- | --- |
| R1: 물리 연결과 비용 확인 | 실제 LG 관측 계약, 입자 기반 관측 연산자, 작은 동역학/gradient/표본추출 시험, 확대 비용 | 물리적인 관측 예측이 가능하고 전체 계산 비용을 산정할 근거가 생겼는가 |
| R2: 실제 주변 현재장 | CF4+은하자료의 전역 z=0 표본·평균·불확실성, 목표 격자1.5 cMpc/h | held-out 자료 예측과 수치/모형 오차가 정량화되었는가 |
| R3: 실제 LG 공동 제약 | 같은 환경 안 MW·M31·M33를 제약한 입자 상태와 <=0.3 지도; LG-on/off 차이 | 성분·관측·밀도장이 실제 연결되고 M33를 생략하지 않았는가 |
| R4: 형성 이력의 정밀 확인 | 표본 이력을 더 정확한 solver로 진화한 현재장·구조 비교 | LG·환경의 체계적 편향, 대응 소실, 재가중 붕괴가 없는가 |
| R5: 줌 초기조건 전달 | 위상 연결된 다중질량 IC, 라그랑지안 mask, RAMSES 시험과 분해능 비교 | 세 성분의 재현·수렴·오염 기준을 충족하는 연구용 앙상블인가 |

### R1 — 가장 먼저 필요한 실제 일

1. 기존 LG 관측값의 질량 정의·기준계·공분산·출처를 한 계약으로 정리한다.
   CF4 자료가 MW·M31·M33의 3차원 속도를 모두 직접 관측한다는 가정은 하지 않는다.
   근거리 흐름 자료를 추가하면 기존 CF4/은하 표본과 중복을 확인한다.
2. MW/M31에는 입자에서 계산하는 부드러운 질량/중심/속도 관측 연산자를 쓰고,
   미분이 어려운 bound-structure 판독은 명시한 target 또는 정밀 비교로 연결한다.
   M33에는 서브구조/결합 상태를 판별하는 연산자가 필요하다. TNG는 교정·검사용이지
   생성장에 정답 component를 제공하지 않는다. 위치-only law는 제안용 후보일 뿐이다.
3. 현재 PMWD는 단일 주기적 mesh이고 bridge는 N64/384에 고정되어 있다.
   **미분 가능한 다중해상도 동역학은 미구현 핵심**이다. 기존 PM 함수를 바탕으로
   2-level force split·서로 다른 입자 질량·padding·coarse feedback의 작은 제어계를
   검증한다. 사용 가능한 검증된 외부 backend가 있으면 재사용하되 접근 가능성을
   먼저 확인한다. 별도 작은 주기 상자는 engineering control이지 실제 환경이 아니다.
4. 한 작은 동일 위상 정밀/근사 비교와 비정답 초기화의 bounded sampling 시험을
   묶는다. 한 사례는 mechanics/비용만 보여준다. 기존 12 cMpc/h MAP 시험에서
   MW·M31·M33까지 회복했다고 주장하지 않는다. 알려진 IC와 대응 출력이 없는 TNG
   현재장만으로 그 비교를 했다고 하지 않는다.

정밀/근사 비교의 작은 버전을 **R2 전에** 수행하여 discrepancy의 크기를 임의로
정하지 않는다. TNG 현재장에서 잘 맞는 연산자도 PM 입자 상태에 그대로 통용된다고
가정하지 않는다. 한 비교는 예비 교정이며, 그 밖의 질량/환경 범위에 대한 정밀도
인증은 아니다. 관측 연산자의 gradient는 같은 작은 계에서 유한차분과 대조한다.

R1 종료 시 두 가지를 실제 증거로 선택한다.

- **동역학:** 미분 가능한 다중해상도 forward를 쓸 수 있는지, 아니면 전역 근사
  추론 + 조건부 정밀 국부 추론/보정이 필요한지 결정한다. 후자는 R3에서 국부
  제안확률·전역 반응·정밀 likelihood를 처리해야 하며, 임의 fine seed 추가로
  대체할 수 없다. R2의 전역 지도 계산 자체가 다중해상도 solver를 요구하는 것은
  아니지만, LG까지 잇는 구현 경로가 없는 채로 큰 R2 계산부터 시작하지 않는다.
- **표본추출:** gradient당 시간과 실효 표본 비용을 따로 측정한다. 짧은 chain에서
  ESS를 안정적으로 추정하지 못하면 비용도 미확정으로 보고한다. 현실적인 계산비를
  벗어나면 사전조건화/표본추출 방식을 재설계하거나 범위를 협의한다. MAP/Laplace로
  바꾸면 별도의 근사이지, 같은 posterior가 이미 확보된 것으로 취급하지 않는다.

첫 수치 묶음의 **계획상 상한은 GPU 합계4시간**(Slurm 대기 제외), 새 대형
snapshot 없이 작은 control에 한정한다. 실제 메모리는 배열·adjoint 크기로
산정하고 예상 peak+20%로 요청한다. 이 상한은 목표 전체의 견적이 아니다.
새 backend 개발까지 4시간에 끝난다는 의미도 아니다. 상한 안에 필요한 정보를
얻지 못하면 미확정 결과와 확대 견적을 보고하며 한도를 자동 연장하지 않는다.
두 번 연속 backend/정밀도 핵심 실패 시 규모를 더 키우지 않고 코드 재사용/협력
가능성과 경로를 재평가한다. 구조 식별과 sampling 비용을 제쳐 둔 채 전역
고분해능 계산으로 넘어가지 않는다.

### R2 — 환경을 다시 실제 관측에 연결

상자는 기존 입력의 **384 cMpc/h**를 기본으로 유지한다. 300 cMpc와 혼용하지
않는다. N128/3은 계산성 점검, N256/1.5는 전역 지도 목표다. nominal grid와
관측 정보가 있는 실효 해상도는 별도로 보고한다. 작은 격자를 만들었다고
자료의 정보가 늘지는 않는다. 관측자 위치·회전·원점을 기존 입력과 맞춘다.

CF4 거리 불확실성/속도 기준계, 은하 바이어스·선택함수·공통 보정변수를
같은 forward model에 연결한다. 현재 선택 CF4 최소거리15.8152 cMpc/h;
은하 counts는 r>=5이고 LG R2 안 직접행이 없다. “15.8까지 모든 자료가 없다”는
해석은 틀리다. Virgo/Coma/void는 위치·질량·흐름·부족밀도 자료의 지원 범위와
중복을 구분한다. Bootes Void도 실제 채택 좌표/범위/선택함수로 포함 여부를
확인하며 상자 안에 이름만 적어 두고 복원됐다고 하지 않는다.

표본 지도, 평균지도, 평균속도에 대한 posterior 표준편차, 물리적 속도분산을
별도 저장한다. 시각화와 held-out 예측을 같은 묶음에서 공개한다. 현 단계의
12 cMpc/h 진단 지도만 더 샘플링하거나 보간하여 완성품으로 삼지 않는다.

### R3 — LG가 실제 장을 바꾸게 한다

정해진 관측 방향/거리/질량/속도가 **진화한 같은 입자 상태**를 제약해야 한다.
잠재 성분 대응과 미검출·공유 peak를 보고한다. MW/M31은 먼저 검증할 수 있지만,
M33가 빠진 결과는 부분 산출물이다. 최종 단계에서는 M33 존재·운동의 관측
likelihood가 상태에 연결되어야 한다. 단순 M33 analogue 출현 비율은 대체물이 아니다.

여기에는 **추론 중의 국부 다중해상도 계산**이 필요하다. 마지막 R5는 그 상태에서
더 정밀한 연구용 IC를 만들어 전달하는 단계이므로, 줌을 모두 R5로 미루는 계획이
아니다. R1의 backend 판정에 따라 미분 가능한 국부 관측 근사를 쓸지, 정밀 입자
관측량을 포함하는 조건부 표본추출/보정을 쓸지 정하고 해당 방법의 오차를 보고한다.
미분 가능한 밀도 봉우리만 만들고 물리적 M33 제약이 끝났다고 하지 않는다.

HOP는 host 후보 판독에 재사용하되 M33에는 서브구조 deblending과 boundness가
필요하다. 추적 ID만으로 생존하는 bound halo를 증명하지 않는다. 선택한 실제
generated 구조로부터 ID를 추적하며, 원자료의 정답 ID를 추론에 넘기지 않는다.
HOP의 그룹 연결 규칙은 서브구조를 합칠 수 있다.
[HOP 원저자 설명](https://lweb.cfa.harvard.edu/~deisenst/hop/).

MW/M31의 M200c, aperture mass와 M33의 bound/enclosed/infall mass를 혼합하지
않는다. 바리온/DMO 차이, 태양/은하 중심 운동, 공통 MW·LMC 영향은 자료나
맞춘 모의 실험의 불확실성으로 처리한다. 수치를 임의로 부풀려 적합을 만들지 않는다.
[TNG 질량·속도 정의](https://www.tng-project.org/data/docs/specifications/).

LG-off → MW/M31-on → M33까지 on은 동일 prior·같은 수치 해상도에서의
정보 비교다. posterior field의 지역 band별 분산/예측 변화와 성분 관측량의
불확실성 변화, 남겨 둔 관측 성분의 예측을 함께 측정한다. 한 개 k_cut 또는
질량 오차 하나로 정보 해상도를 선언하지 않는다. 좌표/관측자 정렬 효과도
같이 통제하고, KL/ESS는 유한표본·상관의 한계를 보고한다.

### R4–R5 — IC를 가진 것과 쓸 수 있는 줌을 구분

사전에 정한 표본 선정 규칙/전체 가중치를 보존하여 정밀 재진화한다. 가장
잘 맞는 seed 하나만 선택해 posterior라 부르지 않는다. solver 변경 뒤 오차가
작으면 같은 상태의 검증 결과로 보고하고, 크면 불일치를 교정하여 다시 추론한다.
큰 likelihood 차이를 소수 표본 재가중으로 감추지 않는다.
재가중을 사용할 경우 중요도 가중치 ESS와 원래 표본의 자기상관을 함께 보고한다.
가중치가 소수 표본에 집중하면 표본 수만 늘린 표를 만들지 않고 새 대상에서 다시
추론한다. 조잡한 solver가 놓친 성분은 사후 가중치만으로 생성되지 않는다.

줌은 z=0 LG 영역과 주변 물질의 **입자 소속을 초기 위치로 추적**하여 만들고,
주기 경계·이웃 shell·완충 영역을 포함한다. 단순 현재 좌표 중심의 구는 mask가
아니다. 이미 LG 관측이 제약한 high-k까지 새 난수로 바꾸지 않는다. 추가 자유도는
고정된 LCDM spectrum과 조건부 위상 계층에 맞게 생성한다.
[다중해상도 IC 방법](https://arxiv.org/abs/1103.6031),
[줌 오염/라그랑지안 영역 연구](https://arxiv.org/abs/1305.6923).

기존 `cf4_zoom_ic2.py`의 공유 주파수 검사만으로 production 적합을 선언하지
않는다. 측정 transfer의 고주파 log-tail, parent 속도 보간, sub-box Fourier
모드와 전역 모드, 2LPT 교차항을 확인하거나 검증된 구현으로 대체해야 한다.
별도 lagMUSIC 프로젝트는 파일/IC 계약 확인 후 재사용 후보이며, 이 계획이
그 프로젝트의 수정 권한이나 검증 완료를 뜻하지 않는다.
소형 무제약 대조군에서 직접 생성한 fine IC와 coarse/fine 합성 IC의 P(k),
교차 스펙트럼과 사용한 2LPT 결합을 비교한다. 이는 재사용 코드의 작은 공학 검사이며
별도 거대 검증 체계가 아니다. CPU 검사도 Syntax 로그인 노드가 아니라 Slurm으로
수행한다. coarse/fine 힘 필터의 합과 독립 난수의 공분산 보존 조건을 혼동하지 않는다.

RAMSES는 물리/질량/힘 분해능 두 단계의 비교를 거친다. DMO가 첫 동역학 검증과
줌 handoff의 기준이고, 실제 별 형성·가스 진화의 수치 모형은 그 다음 별도 설계다.
IC를 전달했다고 은하의 별/가스 진화까지 시뮬레이션한 것은 아니다.
출력 시점/크기/총 저장량, 사용 binary/namelist, 오염률과 세 성분의 수렴을
실행 전에 고정한다. 큰 D production은 별도 승인 범위를 유지한다.

## 5. 해상도와 계산 가능성

아래는 **설계 산술**이며 아직 확보한 실효 분해능이나 측정 비용이 아니다.
전체 물질 DMO 입자에서 `m_p = 2.7754e11 Omega_m (Delta_q)^3 Msun/h`.
예시 Omega_m=0.31; 최종 값은 실제 채택 cosmology로 계산한다.

| 초기 입자 간격 Delta_q (cMpc/h) | 입자 질량 (Msun/h) |
| --- | ---: |
| 0.3 | 2.32e9 |
| 0.1875 | 5.67e8 |
| 0.09375 | 7.09e7 |
| 0.046875 | 8.86e6 |
| 0.0234375 | 1.11e6 |

따라서 **0.1875 지도 ≠ 0.1875 입자 간격 ≠ 헤일로 힘 분해능**이다.
초기 LG 존재/궤도 시험은 대략1e7–1e8 Msun/h부터 범위를 검토하고,
M33의 내부/형성 연구는 ~1e6 Msun/h 이하가 필요할 수 있다. 특정 M33 질량을
가정한 입자 수는 보장이 아니며 실제 작은 질량·tidal stripping에서 수렴을 본다.

L=384에서 RAMSES level19 셀은0.732 ckpc/h, level20은0.366,
level21은0.183이다. kpc 물리값은 a/h를 곱한다. IC level14 간격에서
~1 ckpc/h로 내려가는 데에는 약5개의 추가 level이 필요하며 2–3개가 아니다.
높은 levelmax를 넣는 것만으로 그 level의 셀이 실제 생성되거나 입자 질량이
좋아지는 것은 아니다.

24 cMpc/h 전체 입방체를0.09375로 채우면 약1680만 입자,
0.046875면 약1억3400만 입자다. 작은 LG 목표라도 완충 영역·FFT/gradient
저장은 비용을 크게 늘린다. 전역 N256은1678만 mesh 셀이므로 별도 벤치마크가
필요하다. CPU/GPU 선택은 추정이 아니라 R1의 실제 bytes와 시간을 기반으로 한다.

총 기간은 지금 확정하지 않는다. R1 구현/측정 후 다음 식으로 갱신한다:
`총 GPU 시간 ≈ (gradient/전방 평가당 시간) × (실효 표본당 평가 수)
               × (목표 실효 표본 수) + 정밀 비교/줌 비용`.
평가당 시간이 짧아도 혼합이 나쁘면 전체는 오래 걸린다. 관련 LG 논문의
Appendix C에서도 특정 설정의 독립 표본당 매우 많은 forward 평가가 필요했다.
이를 우리 코드의 실행시간 추정으로 그대로 옮기지 않는다.
[계산량 논의](https://arxiv.org/html/2406.02228v1).
4개 chain을 실행했다거나 1000 step이 끝났다는 것과 유효한 앙상블은 다르다.

### 남은 연구 위험과 축소해서는 안 되는 완료 조건

가장 큰 미해결 위험은 **비선형 전방모형을 통한 표본추출의 비용/혼합**과
**근사 중력장에서 LG 세 성분의 실제 관측량을 예측하는 정확도**다. 다중해상도
backend의 구현·검증과 coarse/fine 피드백도 측정 전에는 해결됐다고 할 수 없다.
R1은 이를 숨기지 않고 일찍 판정하기 위한 묶음이지 성공 보증이 아니다.

최종 전달에는 (1) 실제 자료에 조건부인 현재장/불확실성, (2) 세 성분의 대응과
관측 잔차, (3) 보지 않은 관측의 예측 및 모의 관측에서의 보정 정도,
(4) 높은 정밀도로 다시 진화한 환경/LG 일치, (5) 줌의 위상·질량·오염·수렴 결과가
함께 있어야 한다. 한 장의 그럴듯한 지도나 최선 seed 한 개는 이를 대신하지 않는다.
0.3 셀보다 세밀한 위상을 모두 관측으로 회복해야 한다는 조건은 추가하지 않는다.

## 6. 운영 원칙과 다음 행동

- 운전자: 설계·구현·검사·실행·결과 판정·커밋/푸시. Fable: 중요한 경로 선택,
  발견, 대형 계산에 대한 자문. 자문도 코드/문헌 증거와 대조한다.
- Syntax에서는 Slurm만 사용. 수동 syn101 금지. /gpfs는 공유 저장소이며
  filesystem/inode 검사를 추가하지 않는다. 기존 실패 결과·관련 없는 변경 보존.
- routine 묶음은 사전 승인 범위 안에서 연속 진행하되, 실패한 길의 무한 반복은
  자율진행이 아니다. 목표와 충돌하는 경로 변경은 먼저 설명하고 선택받는다.
- Slurm 안의 이미 정해진 후처리는 dependency/같은 job으로 이어 놓는다.
  동적인 과학 판단은 운전자의 실제 재개가 필요하다. 현재 세션을 다시 깨우는
  기능을 확인하지 않은 채 로그아웃 후 자동 판단/새 계산을 약속하지 않는다.
- **사용자가 내부 IC 공동추론과 R1을 승인했다. R1의 작은 실행부터 진행하며,
  공학 시험을 실제 LG posterior 또는 R1 전체 완료로 승격하지 않는다.**
