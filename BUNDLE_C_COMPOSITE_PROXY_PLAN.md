# C — smallest next implementation: composite LG observation proxy

Proposed2026-09-09 after Fable5 DESIGN CONDITIONAL GO. Awaiting user approval.
Subsequent user approval authorizes this one implementation/execution bundle.
This is the simpler baseline recommended by Q-LEAN, NOT learned cell membership,
a calibrated LG posterior, or a replacement fine-density prior. One deliverable:
field-only candidate mixture + native heldout joint-kinematic diagnostic report.

## Inputs and roles

Reuse job337986 candidate/aperture tables and cached native metadata, original
geometry-only13-train/3-heldout spatial split from spatial_conditional_v1.
Frozen fixture indices: train[0,2,3,4,5,7,8,9,10,11,12,14,15], heldout[16,17,20].
No raw particles, new fields/simulations, source-box scan or storage probes.
These are same-box selected satellite cases, not independent cosmologies.

MW/host anchors are ALL distinct field-only MW_M31 edges already frozen before
truth matching. M33 is a distinct latent galaxy relative to the host, with no
required third grid peak. At inference neither source IDs nor true positions
can enter candidate selection or predictions. Average all candidate-pair
densities with declared weights1/N_pairs, using logsumexp-log(N_pairs).
This is a normalized proxy mixture, NOT a calibrated halo-occupation prior.
Same observer-cell condition as before; no best-pair/oracle selection at test.
Empty lists produce UNRESOLVED_SUPPORT and no usable score; do not fill in truth
or claim a universal likelihood covering that unsupported field.

Use fixed dx aperture moments: positions, mean velocities, three physical
dispersions and mass. The2dx aperture remains descriptive, not tuned. Do not
sum overlapping apertures as disjoint matter or turn either mass into M200c.

## Frozen low-dimensional statistical baseline

For a pair i,j, model four relative three-vectors in BOX-frame physical kpc
and peculiar km/s: r31, r32, v31, v32. Carrier predictions are
[(x_j-x_i), 0, (v_j-v_i), 0]. Host denotes the M31+M33 unresolved composite
proxy, NOT a declaration that its COM is precisely M31's stellar center.

Let e be the pair unit separation, length unit ell=1000*dx/h kpc. Normalize
the four residual rows by [ell,ell,s31,s32], where
s31=sqrt(100^2+mean(sigma_i^2)+mean(sigma_j^2)) km/s and
s32=sqrt(100^2+mean(sigma_j^2)) km/s. These are frozen positive feature scales;
100 km/s is a reference scale, NOT a measured physical dispersion or error
floor added to the field. Report saved physical sigma separately.

Residual mean in each normalized row is beta_g*e (four fitted coefficients).
The joint12D normalized residual covariance is K(4x4) tensor I(3), retaining
cross-role position/velocity correlations with ten free symmetric entries.
Fit beta and the pooled axis covariance once on TRAINING objects with fixed
10% diagonal covariance shrinkage. Positive definiteness required; no tuning
or invented covariance if the fit is singular. Use exact multivariate Gaussian
normalization including all scale determinants. Total14 fitted coefficients;
the isotropy/separable-axis approximation is an explicit strong limitation,
not39 independent universes or measured zero physical tensor cross terms.

Training may use native labels to associate the two HOST roles to frozen
candidate pairs, using the declared one-cell matching convention. Do not run
the earlier three-role competition that assigned a shared host peak to M33.
Report eligible unique host pairs, excluded associations and repeated members;
conditional calibration excludes failed associations and is not completeness.
Require at least8 distinct training pairs; otherwise stop without widening
matching radius. Evaluation uses the mixture, never truth-selected anchors.

Truth r/v target comes from native catalogue centers and massive-member COMs,
not from the aperture or an artificial Gaussian draw. Native masses are bound
SUBFIND members; M33 is the present stripped bound object. No M200c/bound-mass
conversion or actual mass likelihood is fitted in this kinematic baseline.
Gaussian position tails are not a calibrated physical satellite/selection law;
do not reject inconvenient draws and silently drop their normalization.

## One diagnostic job and useful output

One CPU fit/evaluation, no neural learner, no optimizer update ladder:

1. Fit on the frozen training subset, report14 parameters and source counts.
2. Evaluate the full pair-mixture log density at each of three retained native
   LG kinematic targets, not at data generated by this same Gaussian.
3. Compute the fixed3x3 table of those targets scored against all three retained
   field contexts. It probes dependence on F without a mean-field/shuffle outside
   the available source support. It is not calibrated information gain.
4. Report the MW–host marginal and conditional-M33 contribution separately:
   log q(all12 | F) - log q(r31,v31 | F). Marginalize the mixture first; this is
   not the average of per-pair conditional log densities. If total discrimination
   comes only from MW–host geometry, do not attribute it to M33.
5. One fixed noiseless coordinate/observable roundtrip checks BOX-to-declared
   mock ICRS/Solar/Hubble conventions. Keep resolved_halos guard unchanged;
   use a distinctly labelled latent-proxy adapter, never a fake resolved catalogue.
   The statistical test is the12D Cartesian density, not an8D likelihood at
   fixed sky with a missing sky-conditioning normalization/Jacobian.

Output one report with field-context score matrix, role/pair ambiguity and
native versus proxy relative-kinematic plots. Physical sigma and posterior
uncertainty remain distinct. Original samples are native calibration cases,
not actual CF4 or observed LG posterior realizations.

Implementation detail frozen before execution: each eligible unique ordered
MW/host pair receives equal training weight; pooled covariance uses ML3N
denominator before the fixed10% shrinkage. Distinct pairs can share companions;
report repeated member IDs rather than treating them as independent objects.

Fable5 follow-up audit returned **GO for this bounded diagnostic design**,
not a scientific posterior/model certification. It explicitly agreed that
binary-coordinate/anchor implementation would add unnecessary work here.
Record: `config/cf4_composite_proxy_fable5_plan_audit_v1.response.json`.
All three required output conditions are adopted within the same job:

- Report K eigenvalues before/after fixed shrinkage, condition number, eligible
  unique training pairs and exclusions. No tuned repair on nonpositive K.
- In the existing focused normalization test, require identical ell/s31/s32
  conventions in fit and score and all Jacobian scales. In row-major12D units,
  log density = standardized log density minus3*sum(log([ell,ell,s31,s32])).
  Marginal/mixture checks also retain their corresponding physical units.
- Show unsupported/invalid contexts explicitly, not as a low scientific score.
  Empty candidate support or invalid required moments cannot be interpreted as
  evidence against a universe. A merely small Gaussian density is NOT a newly
  invented support cut; do not truncate tails after the fit.

This closes the approved DESIGN bundle. Numerical implementation/execution has
not started; request approval at this next bundle boundary, without a further
audit of these three already-accepted recording requirements.

Reuse existing Gaussian/logsumexp, aperture and observation conventions. Add
only focused checks of mixture normalization/marginals, field-only inference
inputs and coordinate roundtrip; no generic gate or new audit subsystem.
Resources proposed: Slurm2 CPUs,1200 MiB (estimated1000+20%),10min cap, no GPU,
partitions a40/a100/h100/h200 excluding syn06. Read small cached candidate tables
and metadata only; output<5MiB, no full3D duplication. One run, no automatic retry
except a clear implementation defect within this exact experiment.

## Decision and boundary

Numerical completion is distinct from scientific adequacy. Three same-box
heldout objects cannot certify coverage, posterior accuracy or cosmic selection.
State whether host dependence and incremental M33 dependence are visible, weak
or absent; no mandatory positive score and no artificial success threshold.
If useful, next design may connect the diagnostic proxy to a properly specified
field prior/selection model. If not, report which missing dependence prevents
that connection; no automatic high-dimensional learning or extra simulations.

For actual inference, p(E|F,O) or an E-conditioned field prior is still missing,
q_F morphology is not validated, published covariance/stellar/LMC systematics
and actual CF4 overlap remain unresolved. Sharper M33 labels alone do not achieve
the project goal of an LG-conditioned density/velocity field.

## Execution and result (2026-09-09)

Approved implementation/source commitdd55182 was pushed. Slurm337991 executed
on syn05 through the scheduler,23:01:11–23:01:22 KST, COMPLETED exit0 in11s.
CPU2/1200M/10min, no GPU. The three focused tests passed. Logs:
`/gpfs/kjhan/CF4/logs/cf4_lg_composite_337991.{out,err}`.
Products in `/gpfs/kjhan/CF4/z0_density/bundle_c_v1/composite_proxy_v1/`:
`result.json`, `field_context_scores.png`, `native_proxy_kinematics.png`.
Slurm reports3504K batch MaxRSS for this short job; that sampled value is not
a reliable Python peak-memory measurement and is not used to certify a smaller
memory request. No extra memory-monitoring job is warranted.

All13 distinct training pairs were eligible, exclusions0. K eigenvalues after
fixed shrinkage:[.012384,.157142,.257306,.621265], condition number50.17.
Mock coordinate/observable roundtrip max absolute error7.82e-13; this only
checks coordinate conventions, not astronomical accuracy. All9 field-context
scores are supported; none were replaced by an arbitrary low score.

Own-field score minus MEAN score of the other two retained fields, in nats:

| Native target fixture | All12 kinematics | MW–host marginal | M33 conditional addition |
|---|---:|---:|---:|
|16|+23.429|+22.950|+0.479|
|17|+8.774|+10.132|-1.358|
|20|+14.943|+14.966|-0.023|

All12 and host-only scores favor the original field over EACH alternative for
all3 cases. The conditional M33 term does NOT rank the original field first
in any of the3 cases; the positive first-row average contrast is not a win
against both alternatives. This is a pointwise same-box cross-score test, not
Bayes factors for cosmologies, calibrated information gain or reconstructed
phases. The first two plot panels and M33 panel use DIFFERENT color scales.

With all native target kinematics supplied, pair mixture ESS is approximately
1.000/1.000/1.004 (prior candidate counts1352/883/634). This is concentration
of a finite assignment mixture under a noiseless diagnostic, NOT MCMC ESS,
independent universe count or evidence of calibrated unique halo identities.
Equal-pair predictive plots remain broad; they are not LG posterior maps.

**Driver judgement:** implementation/normalization and host-related field
dependence demonstrated at diagnostic level. Incremental M33 field discrimination
is weak/inconsistent with this frozen low-dimensional proxy. Do not promote it
to a useful M33 field constraint merely because the full score prefers the
source field. This does not show that M33 observations are uninformative for a
better physical model. No mass likelihood, separate M33 spatial component,
LG-conditioned field, q_F repair or actual-data posterior has been delivered.

Close this bounded trial without further coefficient/shrinkage/seed tuning.
Preserve the host proxy as a reusable diagnostic. Next design should isolate
what M33 mass/subcell structure or finer LOCAL information can constrain in
the same field, while retaining the missing population-selection/q_F caveats;
do not simply retrain this proxy or launch high-dimensional learning. Plan
audit must ask Q-GOAL/Q-LEAN and all3 identification questions. Await next-bundle
approval; no automatic downstream job remains.
