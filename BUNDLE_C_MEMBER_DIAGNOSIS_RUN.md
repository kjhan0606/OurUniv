# Frozen member diagnosis v5

User-approved2026-09-12; plan `BUNDLE_C_MEMBER_DIAGNOSIS_PLAN.md` preserved.
Fable5 normal completion78825ms, CONDITIONAL GO. Original response/request
in `config/cf4_member_diagnosis_fable5_v1.*`; no fallback audit.

## Audit disposition

Q-GOAL/Q-LEAN accept this ONE frozen evaluation and subsequent role-uncertain
design, not another training or prerequisite of perfect segmentation.

1. Identity metrics reproduce saved map-L1/overlap with rtol1e-4, atol1e-5,
   as in the earlier continuation; GPU name and strict precision recorded.
2. Before launch, material orientation sensitivity means per-role L1 OR
   overlap range>.1 across all48 views. Report full continuous results; this
   is a diagnostic interpretation threshold, NOT scientific acceptance.
   At least2 eligible M31 or conditional M33 choices flags omitted assignment
   uncertainty; not an empirical lower error bound or universal unlearnability.
3. Keep native positive finite role-mass normalization WITHOUT a floor. A
   small M33 denominator is relevant, not an error to hide. Existing positive-
   target guard plus absolute discrepancy/target masses report its scale.
   Added absolute native-map error and identity-map discrepancy in Msun.
   Fable allowed a guard OR floor; arbitrary physical mass flooring rejected.

Audit interpretation that the network may be fitting an assignment mixture
is a hypothesis, not established by this diagnostic. Data multiplicity does
not prove actual fixed13-field input/label collisions. Shared-cell M33 does
not waive observations/criteria or establish permanent unidentifiability.

## Implementation

Reuse load_case, model, metrics, precision and7 previous tests; add one exact
inverse signed-permutation helper/regression. One script streams six fields,
two saved models,48 views each; zero optimizer updates to either checkpoint.
The small existing tests perform synthetic optimization internally, NOT native
training or saved-model updates. Native observation data is not supplied to
this evaluator. No new H5 volume outputs, augmentation ensemble or model choice.
All numerical tests/inference/counts run within1GPU/2CPU/6GiB/15min Slurm,
application13min. Source outputs preserved; new member_mass_diagnosis_v5.
No new training, actual high-resolution posterior or ICs authorized.
