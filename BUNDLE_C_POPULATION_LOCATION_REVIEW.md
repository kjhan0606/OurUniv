# Driver disposition of Fable5 advice

2026-09-12. Exact returned review is preserved in
`config/cf4_population_location_fable5_v1.response.json` (normal138976ms,
claude-fable-5). Verdict APPROVE WITH ESSENTIAL CORRECTIONS. No backup agent,
extra review stage or automatic deference. The adviser saw the complete plan
and supplied evidence, not a code execution or an independent data experiment.

Q-GOAL: agree that a population-weighted candidate position law on the SAME
field addresses a missing interface. It is not yet the observed LG posterior,
member mass/COM law or q_F. A location-only success is not logically necessary
for every alternative route; do not elevate this pilot to a universal gate.
Q-LEAN: one21-coefficient model plus one calibration scalar and one fit is
proportionate. No additional data/profile/snapshot collection is justified now.

Disposition of specific advice:

1. **Reject the claimed time arithmetic error; clarify wording.** The total
   APPLICATION cap110min includes preparation, learning, scoring and sampling.
   Learning70min is a nested cap, not an additional phase. This leaves at
   least10min outside the application under Slurm120min. The code clocks the
   total from program initialization and the learning timer separately. No
   budget expansion or model change; budget-short results remain inconclusive.
2. **Adopt explicit empty-cell rules.** Native zero-mass cell variance/sigma
   and velocity-roughness feature are zero; no division by zero. Neighborhood
   velocity is zero if its mass is zero. No density/physical mass epsilon is
   added. A populated-feature bulk-boost regression covers this convention.
   Negative fixed-filter mass roundoff alone is clipped in derived features,
   not written into the native physical field.
3. **Adopt torus overlap test.** Already implemented with periodic center
   distances in all3 axes and core+feature-halo support, not an ordinary AABB.
   Regression covers boundary wrapping and exact touching versus overlap.
4. **Adopt early counts and thin-slab disclosure.** First substantive data
   operation constructs fixed splits and checks100/30/30 observers before
   feature preparation or fitting. No slab retry. Report actual calibration
   position bounds and its correlated one-box/two-coarse-layer limitation.

Driver also rejects two overstrong inferences in the otherwise useful advice:
a failure does not isolate model capacity as irrelevant, and GPU runtime is
not known to be trivially sufficient before measuring actual preparation and
full alternative-weighted passes. Resource/timing measurements stay inside
the same allocation, without introducing a separate timing/audit ladder.

Driver decision: proceed with the user-approved one-comparison implementation
and Slurm execution after static checks; numerical tests run IN that job.
No externally audited code-pass or scientific GO is claimed before results.
