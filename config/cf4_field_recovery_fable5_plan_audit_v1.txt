You are the independent Fable5 scientific PLAN auditor for OurUniv/CF4.
Read the complete attached plan. Give a decisive, critical audit, not praise.
Tools are disabled; do not claim you inspected data or ran code. The measurements
and code contracts below are driver-reported source evidence, not new results.

Final goal: actual CF4/galaxy/LG observations -> PRESENT z=0 density/velocity
posterior -> LCDM IC -> forward validation -> phase-consistent LG zoom. LG needs
<=0.3 cMpc/h (planned .1875), surroundings1-2 (planned1.5),384 domain. Not direct
CF4->IC, not globally recovered fine phases. User wants short substantive bundles
and an end to excessive diagnostic code/audit micro-stages. Syntax is Slurm.

User approved planning only. Original v2 normalized conservative flow fails all16
development morphology cases despite improved NLL. Fine true-parent/rollout
high-band means on reused heldout cases .650/.390; on training .676/.297.
Existing no-grad sampler uses NumPy conservative decode; proposed all-trace
score-function gradient is implementable without differentiable decoder but may
have very poor variance. The shared model handles six scales and atom branches.
Earlier Fable recommended a bounded paired objective repair with field-only LG
connection first. We did the latter work: field-only three-peak identification
0/32; simple composite proxy has host discrimination but no consistent M33
increment; necessary moment budget all6 alternatives already excluded by hosts.
All three diagnostics are CLOSED, not a reason for endless refinements.

No actual high-resolution posterior exists. q_S/member-selection and global
environment law are missing. The plan refuses to attach a fake likelihood or
copy native membership. The next proposed numerical product is repaired/failed
field prior plus terminal decision, NOT yet observed LG. Evaluate whether that
limited but central step is justified; be candid if this is a dead end.

Required questions: Q-GOAL; Q-LEAN; feasibility; essential versus deferred scope;
MW/M31/M33 roles/ambiguity/unresolved M33 on NEW fields without truth identities;
same-field observation link and unaddressed population selection. Do not require
three resolved peaks or mislabel aperture mass as M200c. Do not silently promote
proxy scores or necessary budget tests to calibrated observation likelihoods.
Give GO/CONDITIONAL GO/NO-GO, with concrete indispensable corrections only.
Avoid prescribing a new audit ladder. If rejecting, specify the smallest viable
central alternative and its assumptions rather than 'more validation needed'.
