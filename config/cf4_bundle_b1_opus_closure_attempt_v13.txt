VERDICT: CONDITIONAL

RISK_LEVEL: MEDIUM — artifact integrity and reproducibility are clean; residual
risk is gate construction and arm stratification, not provenance or safety.

Opus audit of HEAD 823c813:

1. All declared hashes/bytes and scientific firewalls pass. Mode v3 is
   byte-identical when regenerated from the committed source.
2. Analytic Laplace sigma is implemented and tested exactly against the
   covariance used for posterior draws. The analytic result is 0.751953125
   (68%) and 0.966796875 (95%).
3. The pooled one-sided guard is numerically reproduced, but exists only as
   JSON; its power figure used n=512 independent pairs instead of n=64 seed
   clusters, and it lacks a 95-level guard. Per-mode Bonferroni bands are weak.
   Stratification is required because arm widths are deliberately different.
4. Width factors remain in-sample tuned and cannot be used as calibration
   evidence. Validation remains sealed and the strict legacy gate is unchanged.
5. Mark-side misspecification is not yet specified; current mark factors are
   exactly generated by the fitting model. Count-side evidence should be tested
   first.

RECOMMENDATION: Do not open validation or B2. Implement a machine-readable
cluster bootstrap guard with corrected n=64/n=256 sensitivity and a 95-level
component, then perform an arm-stratified count-side sandwich diagnostic. Only
after that preregister a fixed mark-side heavy-tail arm and width-freeze policy.
