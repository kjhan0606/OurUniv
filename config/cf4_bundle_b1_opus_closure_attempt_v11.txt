VERDICT: CONDITIONAL

RISK_LEVEL: MEDIUM — provenance and firewall integrity are clean; residual risk is
methodological because width scalars were tuned on the same development seeds and
the promotion gate still fails.

Opus audit of committed HEAD b7c2676:

1. All recorded source/result/test hashes and bytes match. The pre-width mode
   snapshot is reproducible from commit 0ed3216.
2. count_split is explicitly declared and tested; the seed firewall is disjoint,
   quarantine is mirrored, and validation remains sealed.
3. The exact grouped Sherman-Morrison Fisher, same-truth joint generation,
   canonical/vectorized agreement, optimizer convergence, and D-arm
   Gamma-Poisson variance pass.
4. The D-width repair changes draws only and is honestly reported: strict
   per-voxel pass is 8/64, while the mode diagnostic is 0.7227/0.9512.
5. The across-seed latent-mode metric is correctly defined but must remain
   diagnostic-only. It needs analytic Laplace sigma and a preregistered
   acceptance region before promotion use.
6. The scalar width factors are in-sample tuned on the same 64 seeds; the mode
   coverage number is therefore not out-of-sample calibration evidence.
7. The strict legacy per-voxel gate is not a coverage frequency and remains
   scientifically unsuitable as a promotion gate.
8. Remaining minor items: hash current diagnosis/width/amendment artifacts in the
   record, fix the diagnosis schema label, and clean two dead arithmetic remnants.
9. A mark-side misspecification arm is still required because current marks are
   generated by the exact model that fits them.

RECOMMENDATION: Record CONDITIONAL with promotion NO-GO. Keep validation
[2026083320,2026083576) sealed and do not open B2. Next, replace draw-estimated
sigma with analytic covariance, redesign/freeze the gate, then add a mark-side
misspecification arm using development seeds only.
