VERDICT: NO-GO

- Source/result agree: `src/cf4_b1_integrated_joint_calibration_v3.py` sha256 `14a0b500…0cc29` (22014 B) and `config/…result_v3.json` sha256 `aab9f87f…37328` (8391 B) both match the record file byte-for-byte; the record's own reported metrics equal the result JSON's `aggregate` block.
- Strict gate fails: `strict_low_k_gate_pass_count` = 11 of a required 64 (`strict_low_k_gate_pass: false`), per-arm 3/3/2/3. The record's failure histogram (coverage68 50, coverage95 49) is consistent with the per-arm coverage medians in the result (A 0.805/0.997, D 0.522/0.824) — over-coverage in A/C, under-coverage in the D stress arm.
- Grouped mark Fisher is a correct exact rank-1 Sherman–Morrison: `_single_group_mark_fisher` (src:247-255) gives `J^T(σ²I+τ²11ᵀ)^{-1}J` = `J^TJ/σ² − τ²/(σ⁴(1+τ²n/σ²))·ss^T`, and `_joint_vector_objective` (src:207-213) uses the matching quadratic/log-det form. The `joint_log_likelihood_abs_error` max of 7.6e-10 corroborates agreement with the canonical primitive.
- Development seed firewall holds for the truth seeds: `seed_schedule` is bounded to indices 0-63 → 2026083000-2026083063, exactly the resealed range in `cf4_bundle_b1_seed_firewall_repair_v1.json`; quarantine [2026083064, 2026083128) is not touched, validation [2026083320, 2026083576) reports `validation_opened: false`.
- Firewall caveat: counts/marks/posterior draw from separate namespaces (2026400000/2026500000/2026600000+index) that the repair document does not enumerate as sealed or reserved. Not a contamination, but the firewall record covers only one of four seed streams.
- 17,007 exclusion is enforced as a hard invariant: `_disjoint_target_exclusion_probe` recounts unique non-unmatched `twompp_recno` from the CSV and raises on any change; result reports 17007 with `overlap_count: 0`. The overlap check itself is vacuous by construction (the synthetic count grid carries no target identity, `count_target_identity_count: 0`) — self-documented in-source, so it evidences non-leakage rather than measuring it.
- Optimizer status is clean and is not the blocker: `fit_success_count` 64, `fit_abnormal_termination_count` 0, all members finite-joint and positive-support, all four arms with positive held-out gain. The failure is calibration of the posterior width, not convergence.
- Disposition is correctly held: `observational_z0_posterior: NOT_CREATED`, `KF_EXPAND: NOT_AUTHORIZED`, 256-member validation not run, `0p3_cMpc_h_claim: NOT_ALLOWED` in both source and result; no promotion is supportable on this artifact.

NEXT: Repair the posterior-width model (the `_count_fisher` + `MARK_FISHER` precision used for the 16 draws, and the D-arm stress intensity) using the same 64 development seeds and the frozen mean model and gate, then re-audit before any validation opening or B2.
